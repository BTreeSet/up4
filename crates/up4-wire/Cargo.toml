[package]
name = "up4-wire"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "up4 overlay header codec and sequence accounting (pure, no I/O, no dependencies)"

[dependencies]
