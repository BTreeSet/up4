/* Automatically generated by p4c-ubpf from /home/runner/work/up4/up4/p4/programs/l2fwd/l2fwd.ubpf.p4 on Sun Aug 30 10:13:55 2026
 */
#ifndef _P4_GEN_HEADER_
#define _P4_GEN_HEADER_
#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include "ubpf_common.h"


enum ubpf_action{
ABORT,
DROP,
PASS,
REDIRECT,
};

struct standard_metadata {
    uint32_t input_port; /* bit<32> */
    uint32_t packet_length; /* bit<32> */
    enum ubpf_action output_action; /* ubpf_action */
    uint32_t output_port; /* bit<32> */
    uint8_t clone; /* bool */
    uint32_t clone_port; /* bit<32> */
};

enum HashAlgorithm{
lookup3,
};

struct ethernet_h {
    uint64_t dst; /* bit<48> */
    uint64_t src; /* bit<48> */
    uint16_t ether_type; /* bit<16> */
    uint8_t ebpf_valid;
};

struct headers_t {
    struct ethernet_h ethernet; /* ethernet_h */
};

struct metadata_t {
};


enum ubpf_map_type {
    UBPF_MAP_TYPE_ARRAY = 1,
    UBPF_MAP_TYPE_HASHMAP = 4,
    UBPF_MAP_TYPE_LPM_TRIE = 5,
};
struct ubpf_map_def {
    enum ubpf_map_type type;
    unsigned int key_size;
    unsigned int value_size;
    unsigned int max_entries;
    unsigned int nb_hash_functions;
};

struct pipe_mac_dst_key {
    uint64_t hdr_ethernet_dst; /* hdr.ethernet.dst */
};
enum mac_dst_0_actions {
    pipe_forward,
    pipe_broadcast,
    pipe_drop,
};
struct pipe_mac_dst_value {
    enum mac_dst_0_actions action;
    union {
        struct {
            uint16_t port;
        } pipe_forward;
        struct {
        } pipe_broadcast;
        struct {
        } pipe_drop;
    } u;
};
#if CONTROL_PLANE
static void init_tables() 
{
    uint32_t ebpf_zero = 0;
    {
        struct pipe_mac_dst_value pipe_broadcast_value = {
            .action = pipe_broadcast,
            .u = {.pipe_broadcast = {}},
        };
        INIT_UBPF_TABLE("pipe_mac_dst_defaultAction", sizeof(ebpf_zero), sizeof(pipe_broadcast_value));
        ubpf_map_update(&pipe_mac_dst_defaultAction, &ebpf_zero, &pipe_broadcast_value);
    }
}
#endif
#endif
